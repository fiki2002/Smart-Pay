import 'package:flutter/material.dart';

class VerifyView extends StatelessWidget {
  const VerifyView({super.key});

  static const String route = '/verify_';
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
